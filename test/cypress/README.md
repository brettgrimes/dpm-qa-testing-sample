# Automated testing with Cypress

## To run tests in Cypress in a test runner using a deployed server

```bash
npx cypress open
```

# To run tests in Cypress in a headless browser

```bash
npx cypress run
```
